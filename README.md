# Sample payload placeholder for HTTP hooks

```
{
  "event": "{{Event}}",
  "username": "{{Name}}",
  "filename": "{{ObjectName}}",
  "directory": "{{VirtualDirPath}}",
  "filepath": "{{VirtualPath}}",
  "fsPath": "{{FsPath}}",
  "status": "{{Status}}"
}
```